# pi-agent-sdk Skill — 开发指令

## 定位

Pi Agent SDK 最佳实践 Skill。覆盖 @mariozechner/pi-coding-agent 的核心 API、Extension/Tool 开发、会话管理等。

## 内容维护规则

- reference/ 中的文档跟随 Pi Agent SDK 版本更新
- API 变更时优先更新 reference/，SKILL.md 仅保留快速参考
- 关注 breaking changes：createAgentSession 签名、Provider 注册方式、RPC 协议
